Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \beta_{12} X_1 X_2

# My Math Document

This is an example of an **inline** equation: $\sum_{i=1}^{n} i = \frac{n(n+1)}{2}$, which sits right inside the sentence.

Below is a **block** equation, which centers itself on its own line:

$$
f(x) = \int_{-\infty}^{\infty} e^{-x^2} dx
$$

You can also write complex matrices:

$$
A = \begin{pmatrix}
a & b \\
c & d
\end{pmatrix}
$$

