# Linux system dependencies (Required for Colab environments)
system("apt-get update && apt-get install -y r-cran-pid")

# R package installations
install.packages("DoE.base")   # Creates your experimental design matrix
install.packages("FrF2")       # Manages randomized 2^k factorial runs
install.packages("rsm")        # Builds quadratic models for peak optimization
install.packages("pid")        # Required for the paretoPlot() function
install.packages("daewr")      # Provides standard industry DOE datasets
install.packages("BsMD")       # Box-Meyer methods for fractional analysis
install.packages("ggplot2")    # Professional visualization for your book
