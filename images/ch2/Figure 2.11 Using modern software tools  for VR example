# Expanded canvas size to accommodate large text without margin errors
options(repr.plot.width = 16, repr.plot.height = 9)

# Base parameters with expanded margins
par(mar = c(5, 7, 5, 9), xpd = TRUE, family = "sans", cex = 2)

# 1. Initialize Canvas
plot(0, type = "n", xlim = c(0, 20.5), ylim = c(0.5, 3.5),
     xaxs = "i", yaxs = "i", axes = FALSE, xlab = "", ylab = "")

# 2. Borders
box("figure", col = "black", lwd = 1.2)
box("plot", col = "gray50", lwd = 1)

# 3. Gridlines
abline(v = c(5, 10, 15, 20), col = "#E0E0E0", lwd = 1)
abline(h = c(1, 2, 3), col = "#E0E0E0", lwd = 1)

# 4. Horizontal Effect Bars (Minitab Blue)
rect(xleft = 0, ybottom = 2.5, xright = 2.0, ytop = 3.5, col = "#8DAEE0", border = "gray30", lwd = 1)
rect(xleft = 0, ybottom = 1.5, xright = 1.0, ytop = 2.5, col = "#8DAEE0", border = "gray30", lwd = 1)

# 6. Axes, Ticks, and Axis Labels
axis(2, at = c(1, 2, 3), labels = c("AB", "B", "A"), las = 1, tick = TRUE, col = "gray50", tck = -0.015, cex.axis = 2)
axis(1, at = seq(0, 20, 5), tick = TRUE, col = "gray50", tck = -0.015, cex.axis = 2)
mtext("Effect", side = 1, line = 3.2, cex = 2)
text(x = -3.2, y = 3.75, labels = "Term", font = 2, cex = 2, adj = 0)

# 7. Titles
mtext("Pareto Chart of the Effects", side = 3, line = 2.8, font = 1, cex = 2)
mtext("(response is C7, \u03b1 = 0.05)", side = 3, line = 0.8, col = "gray40", cex = 2)

---

# Set inline display dimensions
options(repr.plot.width = 8, repr.plot.height = 6)

# Configure margins (increased left and bottom margins for larger text)
par(mar = c(5.5, 6.5, 4.5, 2), xpd = TRUE, family = "sans")

# 1. Initialize Canvas (X domain split into two halves: 0-2 for A, 2-4 for B)
plot(0, type = "n", xlim = c(0, 4), ylim = c(5.4, 7.8),
     xaxs = "i", yaxs = "i", axes = FALSE, xlab = "", ylab = "")

# 2. Draw Background Gridlines
abline(h = seq(5.5, 7.5, by = 0.5), col = "#F0F0F0", lwd = 1)
abline(v = c(0.5, 1.5, 2.5, 3.5), col = "#F0F0F0", lwd = 1)

# 3. Draw Center Reference Line (Mean = 6.5)
abline(h = 6.5, col = "gray60", lty = 2, lwd = 1)

# 4. Outer Borders and Panel Dividers
rect(0, 5.4, 4, 7.8, border = "gray50", lwd = 1.2)        # Main outer boundary
segments(2, 5.4, 2, 7.8, col = "gray50", lwd = 1.2)       # Vertical panel divider
segments(0, 7.6, 4, 7.6, col = "gray50", lwd = 1.2)       # Horizontal header divider

# 5. Panel Headers
text(x = 1, y = 7.7, labels = "A", font = 1, cex = 2)
text(x = 3, y = 7.7, labels = "B", font = 1, cex = 2)

# 6. Plot Data Points and Lines (Minitab Dark Blue)
minitab_blue <- "#164DB4"

# Factor A (Mapped to x = 0.5 and x = 1.5)
lines(c(0.5, 1.5), c(5.5, 7.5), col = minitab_blue, lwd = 1.5)
points(c(0.5, 1.5), c(5.5, 7.5), pch = 16, col = minitab_blue, cex = 2)

# Factor B (Mapped to x = 2.5 and x = 3.5)
lines(c(2.5, 3.5), c(6.0, 7.0), col = minitab_blue, lwd = 1.5)
points(c(2.5, 3.5), c(6.0, 7.0), pch = 16, col = minitab_blue, cex = 2)

# 7. Axes and Labels (cex.axis = 2 makes the numbers bigger)
# Y-axis 
axis(2, at = seq(5.5, 7.5, by = 0.5), pos = 0, las = 1, col = NA, col.ticks = "gray50", col.axis = "black", tck = -0.015, cex.axis = 2)
mtext("Mean", side = 2, line = 4.2, cex = 2)

# X-axis (Panel A ticks)
axis(1, at = c(0.5, 1.5), labels = c("-1", "1"), pos = 5.4, col = NA, col.ticks = "gray50", col.axis = "black", tck = -0.015, cex.axis = 2)
# X-axis (Panel B ticks)
axis(1, at = c(2.5, 3.5), labels = c("-1", "1"), pos = 5.4, col = NA, col.ticks = "gray50", col.axis = "black", tck = -0.015, cex.axis = 2)

# 8. Main Titles
mtext("Main Effects Plot", side = 3, line = 2.2, font = 1, cex = 2)

---

# Set inline display dimensions
options(repr.plot.width = 8, repr.plot.height = 6)

# Configure margins (extra right margin for legend)
par(mar = c(5.5, 6.5, 4.5, 7.5), xpd = TRUE, family = "sans")

# 1. Initialize Canvas
plot(0, type = "n", xlim = c(-1.5, 1.5), ylim = c(4.8, 8.2),
     xaxs = "i", yaxs = "i", axes = FALSE, xlab = "", ylab = "")

# 2. Draw Background Gridlines
abline(h = seq(5.0, 8.0, by = 0.5), col = "#F0F0F0", lwd = 1)
abline(v = c(-1.0, 1.0), col = "#F0F0F0", lwd = 1)

# 3. Outer Border and Panel Header Box
rect(-1.5, 4.8, 1.5, 8.2, border = "gray50", lwd = 1.2)
rect(-1.5, 8.0, 1.5, 8.2, col = "#EFEFEF", border = "gray50", lwd = 1.2)
text(x = 0, y = 8.1, labels = "A * B", font = 1, cex = 2)

# 4. Plot Interaction Lines and Points
# Line B = -1.0 (Solid Blue Line)
lines(c(-1.0, 1.0), c(5.0, 7.0), col = "#3182bd", lwd = 1.5)
points(c(-1.0, 1.0), c(5.0, 7.0), pch = 16, col = "#3182bd", cex = 2)

# Line B = 1.0 (Red Dashed Line with Square Points)
lines(c(-1.0, 1.0), c(6.0, 8.0), col = "#de2d26", lwd = 1.5, lty = 2)
points(c(-1.0, 1.0), c(6.0, 8.0), pch = 15, col = "#de2d26", cex = 2)

# 5. Axes and Labels
axis(2, at = seq(5.0, 8.0, by = 0.5), pos = -1.5, las = 1, col = NA, col.ticks = "gray50", col.axis = "black", tck = -0.015, cex.axis = 2)
mtext("Mean", side = 2, line = 4.2, cex = 2)

axis(1, at = c(-1.0, 1.0), labels = c("-1.0", "1.0"), pos = 4.8, col = NA, col.ticks = "gray50", col.axis = "black", tck = -0.015, cex.axis = 2)
mtext("A", side = 1, line = 3.5, cex = 2)

# 6. Main Titles
mtext("Interaction Plot", side = 3, line = 2.4, font = 1, cex = 2)

# 7. Smaller Legend Box on the Right
rect(1.7, 7.3, 2.5, 8.1, col = "white", border = "gray50", lwd = 1)
text(x = 2.1, y = 7.9, labels = "B", cex = 0.9)

lines(c(1.8, 2.0), c(7.6, 7.6), col = "#3182bd", lwd = 1.5)
points(1.9, 7.6, pch = 16, col = "#3182bd", cex = 1)
text(x = 2.1, y = 7.6, labels = "-1.0", adj = 0, cex = 0.8)

lines(c(1.8, 2.0), c(7.4, 7.4), col = "#de2d26", lwd = 1.5, lty = 2)
points(1.9, 7.4, pch = 15, col = "#de2d26", cex = 1)
text(x = 2.1, y = 7.4, labels = "1.0", adj = 0, cex = 0.8)
